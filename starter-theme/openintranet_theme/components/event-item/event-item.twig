{#
/**
 * @file
 * Event item component.
 *
 * This component is included at the view level to attach its CSS and JS
 * assets. The actual event-item markup is composed by Views Custom Text fields.
 * The JS makes each .event-item row fully clickable by detecting the <a> in
 * .title and navigating on row click.
 */
#}
{# Component is used only for asset attachment; no markup needed here. #}
