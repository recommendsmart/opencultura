langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_consultation_deadline
    - node.type.consultation
  module:
    - datetime
id: node.consultation.field_consultation_deadline
field_name: field_consultation_deadline
entity_type: node
bundle: consultation
label: Deadline
description: 'Set the final date by which all reviews must be completed and a decision made. Consider reviewer availability and complexity of the consultation. Default is 30 days from creation. This date will be used to track overdue consultations.'
required: true
translatable: false
default_value:
  -
    default_date_type: relative
    default_date: '+30 days'
default_value_callback: ''
settings: {  }
field_type: datetime
